<%= form_with(model: zak_spec) do |form| %>
  <% if zak_spec.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zak_spec.errors.count, "error") %> prohibited this zak_spec from being saved:</h2>

      <ul>
        <% zak_spec.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
